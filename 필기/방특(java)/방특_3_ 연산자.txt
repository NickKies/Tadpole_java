a = 1;  대입연산자 = (순수대입연산자)
a+=1; a = a + 1;  대입 연산자 =와 산술연산자 +의 결합
a-=1; a = a - 1;  연산자 두개 (-,=)를 여러개 붙인다는 의미에서 복합대입 연산자라고 한다.
a*=1; a = a * 1;
a/=1; a = a / 1;
a%=1; a = a % 1;
 
java에서는 기본자료형으로 선언된 변수외에는 일반적인 class 로부터 만들어진 객체
의 주소를 대입하여 참조변수를 사용하게 된다.
그러므로 참조변수의 값은 객체의 주소값이 된다.