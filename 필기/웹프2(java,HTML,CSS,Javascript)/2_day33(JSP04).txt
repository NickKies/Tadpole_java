쿠키(Cookie)

웹 브라우저가 보관하고 잇는 데이터로, 웹 서버에 요청을 보낼 때 쿠키를 헤더에 담아서 전송하게 됩니다. 웹 브라우저는 쿠기가 삭제되기 전까지 웹 서버에 쿠키를 전송합니다.

동작방식

1. 쿠키 생성 : 웹 서버에서 쿠키를 생성하고 쿠키에 응답데이터를 담아서 웹 브라우저에 전송합니다.
2. 쿠키 저장 : 웹 브라우저는 응답 데이터를 담고 있는 쿠키를 메모리나 파일로 저장합니다.
3. 쿠키 전송 : 웹 브라우저는 쿠키를 요청이 있을 때마다 웹 서버애 전송합니다. 웹 서버는 쿠키를 사용해서 필요한 작업을 수행할 수 있습니다.

쿠키 생성 방식

1. 쿠키를 생성하고 response 객체에 담아줍니다.

	Cookie cookie = new Cookie("쿠키 이름", "쿠키값");
	response.addCookie(cookie);     <- 서버에서 보냄

2. 쿠키의 값을 얻어옵니다.

	Cookie[] cookies = request.getCookies();    // 심어놨던 쿠키 배열을 반환하고 없으면 null을 반환.



문제 1

Cookie2P.jsp 에서 다음과 같이 작성

1. 쿠키를 생성하여 userid와 pwd를 쿠키에 저장
2. Cookie2P_1.jsp 로 이동 (response.sendRedirect())


Cookie2P_1jsp에서 다음과 같이 작성

1. 저장된 쿠키 값을 출력합니다.

아이디 : xxxxx
패스워드 : xxxxx



문제 2
1.
- Cookie2P.jsp 에서 아이디가 "apple" 이고 패스워드가 "1111"인 사용자일 경우 쿠키를 생성
- 아이디가 "apple"이고 패스워드가 "1111"이 아닐 경우 로그인 페이지로 이동
	alert("아이디 또는 패스워드 입력을 확인해 주세요")
- 로그인이 되었다면 다시 Cookie2.jsp로 이동


2. Cookie2.jsp에서 쿠키가 존재한다면 아래와 같은 화면을 보여줍니다.

apple님 환영합니다.
 아이디 : apple
  [로그아웃]
















